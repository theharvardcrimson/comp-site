*.sw[op]
*.pyc
rendered
.DS_Store
s3_website.yml
_site

# Pre-redesign reference screenshots (~58MB). Local-only, never committed.
# Regenerate instructions live in CLAUDE.md.
_baseline

# Source artwork stays out of the repo. The supplied logo was a 22800x22800
# 18MB JPEG and .ai is an authoring format, not a web one. Keep masters
# somewhere else; the optimised images/logo-mark.png is what ships.
*.ai
final_logo.*
new_hero.*
new_new_hero.*

# Photo masters as supplied: 6000x4000 frames and full-size cover scans, 60MB+.
# The repo is public and these are large; keep your masters in Drive/Photos.
# Only the optimised web versions are committed (images/covers/, images/hero.jpg).
# NOTE: this keeps them out of git, NOT out of the build -- _config.yml's
# `exclude` is what stops Jekyll publishing them.
images/gallery/
